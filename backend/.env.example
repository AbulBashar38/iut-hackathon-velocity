# Server environment
NODE_ENV=development

# Port the API + Socket.io server listens on
PORT=6006

# Allowed origin for CORS and Socket.io (the frontend dev server)
CLIENT_ORIGIN=http://localhost:5173
